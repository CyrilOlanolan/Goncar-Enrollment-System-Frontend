import { InputSelect } from '../../components/ComponentIndex';

export default {
  title: 'Shared/Input Select',
  component: InputSelect,
};

export const Main = {
  args: {
    label: "Date of Birth"
  }
};